# cALorie web application

Next.js frontend and server routes for signed uploads, job creation and result
polling. See the repository root README for setup and architecture.

```bash
cp .env.example .env.local
npm ci
npm run dev
```
